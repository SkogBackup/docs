---
prompt: intro
---

[$prompt:intro]
# Intro

Here we will add things which are not worth its own prompt or where the prompt is not ready yet.

## Tags

To document or display tags in the style of: [!@command]
When the ! is removed from [!@command] it will be parsed for real.
Example of real command:
[@todo:parse:create a example command which gets skipped by parses]

[/$prompt:intro]
