---
categories:

1. **Software Development**: The file appears to be related to software development best practices and design principles.
2. **Development Frameworks**: As the file is part of the Axios project, it can also be categorized under development frameworks.
tags:

1. **JavaScript**: The file specifically mentions JavaScript (via axios) as a relevant tag.
2. **Software Design**: The discussion around core modules and their specific domain logic makes this topic related to software design principles.
3. **Development Frameworks**: As mentioned earlier, the file is part of the Axios project, making it relevant to development frameworks.

These categories and tags seem to be relevant to the content of the memory file, which discusses the role of core modules in a JavaScript development framework (Axios).
---
# axios // core

The modules found in `core/` should be modules that are specific to the domain logic of axios. These modules would most likely not make sense to be consumed outside of the axios module, as their logic is too specific. Some examples of core modules are:

- Dispatching requests
  - Requests sent via `adapters/` (see lib/adapters/README.md)
- Managing interceptors
- Handling config
