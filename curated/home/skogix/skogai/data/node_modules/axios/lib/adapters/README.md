---
categories:

1. Software Development
2. Front-end Development (specifically, Client-side Request/Response Handling)
3. JavaScript (due to the use of Axios and ES6 syntax)
tags:

1. JavaScript
2. Axios
3. Client-side Request/Response Handling
4. Promise-based Programming
5. Request/Response Adapters

These categories and tags capture the essence of the file, which appears to be a custom adapter module for Axios that handles request dispatching and response settling.
---
# axios // adapters

The modules under `adapters/` are modules that handle dispatching a request and settling a returned `Promise` once a response is received.

## Example

```js
var settle = require('./../core/settle');

module.exports = function myAdapter(config) {
  // At this point:
  //  - config has been merged with defaults
  //  - request transformers have already run
  //  - request interceptors have already run
  
  // Make the request using config provided
  // Upon response settle the Promise

  return new Promise(function(resolve, reject) {
  
    var response = {
      data: responseData,
      status: request.status,
      statusText: request.statusText,
      headers: responseHeaders,
      config: config,
      request: request
    };

    settle(resolve, reject, response);

    // From here:
    //  - response transformers will run
    //  - response interceptors will run
  });
}
```
