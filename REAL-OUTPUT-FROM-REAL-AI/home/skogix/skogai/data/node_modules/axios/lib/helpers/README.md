---
categories:

* Software Development
* Project Management
* Technical Writing (the format and style suggest a documentation or wiki-style page)
tags:

* Development (more specifically, software development)
* Technology (specifically, web development using Axios)
* Documentation

Note that there are no specific programming languages mentioned in the content, but based on the context of the file, it is likely that the modules described are written in JavaScript.
---
# axios // helpers

The modules found in `helpers/` should be generic modules that are _not_ specific to the domain logic of axios. These modules could theoretically be published to npm on their own and consumed by other modules or apps. Some examples of generic modules are things like:

- Browser polyfills
- Managing cookies
- Parsing HTTP headers
