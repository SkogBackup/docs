#!/usr/bin/env bash
#
# Wrapper to run docgen-process.py with uv (for dependencies)
#
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_DIR="$(dirname "$SCRIPT_DIR")"

exec uv run --project "$PROJECT_DIR" python3 "$SCRIPT_DIR/docgen-process.py" "$@"
